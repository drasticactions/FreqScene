name: CI

on:
  push:
    branches: [main]
    tags: ['v*']
    # Skip a build when a push touches ONLY these (docs, non-code). Any other
    # changed path — source, csproj, native/pack scripts, this workflow — builds.
    # Note: paths-ignore does not apply to tag pushes (v* always builds a release)
    # or to workflow_dispatch (manual runs always build).
    paths-ignore:
      - '**/*.md'
      - 'LICENSE.txt'
      - '.gitignore'
      - '.gitattributes'
  pull_request:
    branches: [main]
    paths-ignore:
      - '**/*.md'
      - 'LICENSE.txt'
      - '.gitignore'
      - '.gitattributes'
  workflow_dispatch:

# Cancel superseded runs on the same branch/PR (but let tag builds finish).
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: ${{ !startsWith(github.ref, 'refs/tags/') }}

permissions:
  contents: read

jobs:
  # ---------------------------------------------------------------------------
  macos:
    name: macOS (universal, adhoc-signed)
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json

      - name: Resolve tag version
        id: tagver
        if: startsWith(github.ref, 'refs/tags/v')
        run: echo "version=${GITHUB_REF#refs/tags/v}" >> "$GITHUB_OUTPUT"

      - name: Build .app bundle (universal, NativeAOT, ad-hoc signed)
        run: |
          eng/pack/build-macos-app.sh \
            --sign adhoc \
            --arch universal \
            --aot \
            --version "${VERSION}"
        env:
          VERSION: ${{ steps.tagver.outputs.version || '1.0.0' }}

      - uses: actions/upload-artifact@v4
        with:
          name: freqscene-macos-universal
          path: artifacts/pack/*.zip
          if-no-files-found: error

  # ---------------------------------------------------------------------------
  linux:
    name: Linux x64 (AppImage)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json

      - name: Install native build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            cmake build-essential patchelf zsync \
            clang zlib1g-dev imagemagick \
            libgl1-mesa-dev libglu1-mesa-dev

      - name: Resolve tag version
        id: tagver
        if: startsWith(github.ref, 'refs/tags/v')
        run: echo "version=${GITHUB_REF#refs/tags/v}" >> "$GITHUB_OUTPUT"

      - name: Build AppImage (NativeAOT)
        # APPIMAGE_EXTRACT_AND_RUN lets the appimagetool AppImage run without FUSE,
        # which GitHub-hosted runners do not provide.
        run: |
          eng/pack/build-linux-appimage.sh \
            --arch x64 \
            --aot \
            --version "${VERSION}"
        env:
          APPIMAGE_EXTRACT_AND_RUN: "1"
          VERSION: ${{ steps.tagver.outputs.version || '1.0.0' }}

      - uses: actions/upload-artifact@v4
        with:
          name: freqscene-linux-x64
          path: |
            artifacts/pack/*.AppImage
            artifacts/pack/*.zsync
          if-no-files-found: error

  # ---------------------------------------------------------------------------
  # freqscene-kiosk — the Linux-only headless head. Like the desktop app it ships
  # native libprojectM (it creates a ProjectM instance), so it needs the full
  # CMake/GL toolchain and the recursive submodules (external/projectm for the
  # native build, external/MagicOnion via FreqScene.Core -> FreqScene.Remote.Server).
  kiosk-linux:
    name: Kiosk Linux x64 (AOT tarball)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json

      - name: Install native build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            cmake build-essential patchelf \
            clang zlib1g-dev \
            libgl1-mesa-dev libglu1-mesa-dev

      - name: Resolve tag version
        id: tagver
        if: startsWith(github.ref, 'refs/tags/v')
        run: echo "version=${GITHUB_REF#refs/tags/v}" >> "$GITHUB_OUTPUT"

      - name: Build kiosk tarball (NativeAOT)
        run: eng/pack/build-kiosk.sh --arch x64 --version "${VERSION}"
        env:
          VERSION: ${{ steps.tagver.outputs.version || '1.0.0' }}

      - uses: actions/upload-artifact@v4
        with:
          name: freqscene-kiosk-linux-x64
          path: artifacts/pack/*.tar.gz
          if-no-files-found: error

  # ---------------------------------------------------------------------------
  windows:
    name: Windows x64 (AOT zip)
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json

      - name: Resolve tag version
        id: tagver
        if: startsWith(github.ref, 'refs/tags/v')
        shell: bash
        run: echo "version=${GITHUB_REF#refs/tags/v}" >> "$GITHUB_OUTPUT"

      - name: Build Windows zip (NativeAOT)
        shell: pwsh
        run: |
          eng/pack/build-windows-zip.ps1 -Arch x64 -Version $env:VERSION
        env:
          VERSION: ${{ steps.tagver.outputs.version || '1.0.0' }}

      - uses: actions/upload-artifact@v4
        with:
          name: freqscene-windows-x64
          path: artifacts/pack/*.zip
          if-no-files-found: error

  # ---------------------------------------------------------------------------
  # FreqScene.Controller — the terminal head. Shipped as a NativeAOT binary in a
  # plain archive: no app bundle, no AppImage, and no native libprojectM (the
  # controller never creates a ProjectM instance), so these jobs need no CMake
  # toolchain and skip the (large) external/projectm submodule. They still need
  # external/MagicOnion, which FreqScene.Remote* consume as ProjectReferences —
  # hence the targeted init below instead of `submodules: recursive` on checkout.
  controller-macos:
    name: Controller macOS (universal, adhoc-signed)
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - name: Check out MagicOnion submodule
        run: git submodule update --init external/MagicOnion

      - uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json

      - name: Resolve tag version
        id: tagver
        if: startsWith(github.ref, 'refs/tags/v')
        run: echo "version=${GITHUB_REF#refs/tags/v}" >> "$GITHUB_OUTPUT"

      - name: Build controller tarball (universal, NativeAOT, ad-hoc signed)
        run: eng/pack/build-controller.sh --os macos --arch universal --version "${VERSION}"
        env:
          VERSION: ${{ steps.tagver.outputs.version || '1.0.0' }}

      - uses: actions/upload-artifact@v4
        with:
          name: freqscene-controller-macos-universal
          path: artifacts/pack/*.tar.gz
          if-no-files-found: error

  # ---------------------------------------------------------------------------
  controller-linux:
    name: Controller Linux x64 (AOT tarball)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Check out MagicOnion submodule
        run: git submodule update --init external/MagicOnion

      - uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json

      - name: Install NativeAOT build dependencies
        # NativeAOT links with clang against zlib; no CMake/GL packages needed
        # here because the controller ships no native libprojectM.
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            clang zlib1g-dev

      - name: Resolve tag version
        id: tagver
        if: startsWith(github.ref, 'refs/tags/v')
        run: echo "version=${GITHUB_REF#refs/tags/v}" >> "$GITHUB_OUTPUT"

      - name: Build controller tarball (NativeAOT)
        run: eng/pack/build-controller.sh --os linux --arch x64 --version "${VERSION}"
        env:
          VERSION: ${{ steps.tagver.outputs.version || '1.0.0' }}

      - uses: actions/upload-artifact@v4
        with:
          name: freqscene-controller-linux-x64
          path: artifacts/pack/*.tar.gz
          if-no-files-found: error

  # ---------------------------------------------------------------------------
  controller-windows:
    name: Controller Windows x64 (AOT zip)
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v4

      - name: Check out MagicOnion submodule
        run: git submodule update --init external/MagicOnion

      - uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json

      - name: Resolve tag version
        id: tagver
        if: startsWith(github.ref, 'refs/tags/v')
        shell: bash
        run: echo "version=${GITHUB_REF#refs/tags/v}" >> "$GITHUB_OUTPUT"

      - name: Build controller zip (NativeAOT)
        shell: pwsh
        run: |
          eng/pack/build-controller.ps1 -Arch x64 -Version $env:VERSION
        env:
          VERSION: ${{ steps.tagver.outputs.version || '1.0.0' }}

      - uses: actions/upload-artifact@v4
        with:
          name: freqscene-controller-windows-x64
          path: artifacts/pack/*.zip
          if-no-files-found: error

  # ---------------------------------------------------------------------------
  # On a v* tag, gather every platform's artifacts and publish a GitHub Release.
  release:
    name: Publish release
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [macos, linux, windows, kiosk-linux, controller-macos, controller-linux, controller-windows]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ github.ref_name }}
        run: |
          gh release create "$TAG" \
            --repo "$GITHUB_REPOSITORY" \
            --title "$TAG" \
            --generate-notes \
            $(find dist -type f | sort)
